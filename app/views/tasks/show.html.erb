<h1><%= @task.title %></h1>

<h3><%= link_to 'Edit task', edit_task_path %></h3>

<p><%= @task.details %></p>
<p><input type="checkbox" <%= 'checked' if @task.completed %>> <%= @task.completed ? 'This task is completed' : 'This task is not completed yet' %></p>
<%= link_to 'Back to list', tasks_path %>
